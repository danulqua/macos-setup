ZSH_CONFIG_DIR="${XDG_CONFIG_HOME:-$HOME/.config}/zsh"

zsh_config_files=(
  path
  aliases
  functions
  completion
)

for zsh_config_file in "${zsh_config_files[@]}"; do
  config_path="$ZSH_CONFIG_DIR/$zsh_config_file.zsh"
  [[ -r "$config_path" ]] && source "$config_path"
done

# Machine-specific environment variables and secrets.
[[ -r "$ZSH_CONFIG_DIR/secrets.zsh" ]] &&
  source "$ZSH_CONFIG_DIR/secrets.zsh"

# Tool initialization must come last.
[[ -r "$ZSH_CONFIG_DIR/tools.zsh" ]] &&
  source "$ZSH_CONFIG_DIR/tools.zsh"

unset ZSH_CONFIG_DIR zsh_config_files zsh_config_file config_path
